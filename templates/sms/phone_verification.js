export const message = (otp) => {
  return (
    `Dear User,\n` +
    `${otp} is your otp for phone verification. Please enter the OTP to verify your phone number.\n` +
    `Regards\n` +
    `SinghAstra`
  );
};
